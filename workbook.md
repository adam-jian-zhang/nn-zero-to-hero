# setup

```bash

pip install pysocks

pip install numpy
pip install matplotlib
pip install graphviz

# or if behind proxy
# https_proxy=socks5://localhost:7777 pip install numpy

# python 3.9 venv
pip install torch torchvision

pip freeze > requirements.txt
```
